# PDF 转 Word

## 用法

拖 `.pdf` 到 **run.bat**，或双击 run.bat。

## 含图片的 PDF

- **文字为主的页**：只提取文字，不重复插入页内多张内嵌图（避免 6 页 PDF 变成几十页 Word）。
- **扫描/纯图页**：每 PDF 页只插入**一张**整页预览图（无 `[第 N 页]` 等占位文字）。

版式与原件可能不一致；若需高还原，请用专业软件打开 PDF 另存，或先 `pdf2image` 再手工排版。

## 进度

转换时命令行会显示进度条（按页）。
