# PDF 加密

## 使用方法
1. 首次：运行上级目录 **install.bat**
2. 将 PDF **拖到本文件夹「运行.bat」**
3. 或双击「运行.bat」按提示操作（image2pdf 可多选图）

## 输出
- 默认生成 _encrypted.pdf（与源文件同目录）

## 命令行
```bat
cd ..
python core\pdf_tool.py encrypt "文件路径"
```
